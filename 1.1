def fibonacci(n):
    cur = 1
    if n > 2:
        cur = fibonacci(n-1) + fibonacci(n-2)
    return cur

element = int(input('Enter the number of the item you are looking for : '))
value = fibonacci(element)
print(str(element)+' sequence element is ' + str(value))

